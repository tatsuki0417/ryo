# FolderCraft の依存
# コア機能（テキスト/連番でのフォルダ作成・CLI）は標準ライブラリだけで動きます。
# 以下は「スクショOCR」「画像貼り付け」「画面キャプチャ」を使う場合に必要です。

Pillow>=9.0        # 画像の読み込み・クリップボード・画面キャプチャ
pytesseract>=0.3.10  # Tesseract OCR の Python ラッパー（別途 Tesseract 本体が必要）
